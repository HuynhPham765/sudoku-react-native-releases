# sudoku-react-native-releases

OTA release manifest (`latest.json`) + APK downloads for the Sudoku React Native app (private source repo: `HuynhPham765/caro-react-native`, branch `sudoku-production`).

Used by `src/services/updater.ts` for the in-app "Kiểm tra cập nhật" feature (Android-only, phase 1).
